// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyBeCIvAv6B6zfmtnC2Qvqp_c3v13EjEk8s",
    authDomain: "timesheetapp-1b83f.firebaseapp.com",
    databaseURL: "https://timesheetapp-1b83f.firebaseio.com",
    projectId: "timesheetapp-1b83f",
    storageBucket: "timesheetapp-1b83f.appspot.com",
    messagingSenderId: "793965299471"
  }
};

/*  
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
